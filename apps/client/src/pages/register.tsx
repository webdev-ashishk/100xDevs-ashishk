import { SignUp } from "ui";

const register = () => {
  return (
    <div style={{ color: "red" }}>
      <h1> register page rendered!</h1>
      <SignUp />
    </div>
  );
};

export default register;
